import { NewsletterEditor } from '../components/NewsletterEditor';

export const metadata = {
  title: 'Create Newsletter Campaign | Admin',
};

export default function CreateNewsletterPage() {
  return <NewsletterEditor />;
}
